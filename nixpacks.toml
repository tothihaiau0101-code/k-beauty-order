providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -r requirements.txt"]

[phases.start]
cmd = ". /opt/venv/bin/activate && python tools/telegram_bot.py"
